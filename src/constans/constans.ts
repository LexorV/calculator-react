export enum NameComponents {
  result = 'result',
  numbers = 'numbers',
  operators = 'operators',
  equals = 'equals',
}
export enum Operators {
  delete = '/',
  multiplication = '*',
  minus = '-',
  plus = '+',
}
export const ColorMain = '#F3F4F6';
